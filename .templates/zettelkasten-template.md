**`id`**: <% tp.file.title.split("-").slice(0, 3).join("-") %> **`title`**: <% tp.file.title.split("-").slice(3).join("-") %> **`date`**: <% tp.date.now("YYYY-MM-DD") %> **`tags`**:

---

###### Content

###### References
